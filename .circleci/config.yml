version: 2

jobs:
  build:
    machine: true
    steps:
      - checkout
      - run:
          name: install dzil
          command: |
            perl -e "print 0;"
      - run:
          name: dzil test 
          command: |
            pwd
            cd home/circleci/project
            make test