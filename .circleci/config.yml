version: 2

jobs:
  build:
    machine: true
    steps:
      - checkout
      - run:
          name: install dzil
          command: |
            perl -e "print 0;"
      - run:
          name: dzil test 
          command: |
            perl Makefile.PL
            ls
            cat Makefile
            make test